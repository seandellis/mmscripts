if(documents.length > 0){	    var doc = activeDocument;	var activeLayer = doc.activeLayer;		if(activeLayer.typename=="LayerSet"){		        var layerNum = activeLayer.layers.length;				if(layerNum>0){				runProcessing(activeLayer);		}else{			alert("No layers inside");		}			}else{				alert("Select a Layer Set");			}		}else{	alert("Open a Document");}function runProcessing(srcLevel){	for(var i=layerNum-1; i>=0; i--){        var lyr = srcLevel.layers[i];        lyr.visible = false;      }    for(i=layerNum-1; i>=0; i--){        lyr = srcLevel.layers[i];        lyr.visible = true;        doc.layerComps.add( lyr.name, "", false, false, true);        if(i!=0) lyr.visible = false;	}}doc = null;activeLayer = null;layerNum = null;